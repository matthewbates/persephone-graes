import styled from "styled-components";

export const GoogleStyles = styled.div`
  display: flex;
  margin: 10px 50px;
  filter: drop-shadow(0, 0, 0, 0.2 #fff);
  margin-top: 20px;
  justify-content: center;

`;
